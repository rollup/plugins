{
  "name": "@rollup/plugins",
  "private": true,
  "scripts": {
    "test": "pnpm recursive run test",
    "test:pkg": "pnpm run test --filter"
  },
  "devDependencies": {
    "eslint-config-rollup": "^0.1.0",
    "execa": "^2.0.4",
    "pnpm": "3"
  }
}
